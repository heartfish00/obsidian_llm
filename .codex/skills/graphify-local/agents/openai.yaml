interface:
  display_name: "Graphify Local"
  short_description: "Build/query Obsidian graph DB with optional Search X"
  default_prompt: "Use $graphify-local to rebuild the local Obsidian graph database, query it for a graph.html path, or add --search-x when X/Twitter realtime context is requested."
